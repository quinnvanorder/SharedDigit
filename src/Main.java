/*
Write a method named hasSharedDigit with two parameters of type int
each number should be within the range of 10 - 99. If either is not within range return false.
The method should return true if there is a digit that appears in both numbers, such as 2 in 21 and 12, otherwise return false.

 */
public class Main {
    public static void main(String[] args) {
        System.out.println(SharedDigit.hasSharedDigit(44,23));
    }
}
